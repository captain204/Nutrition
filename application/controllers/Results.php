<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Results extends CI_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Results_model');
        $this->load->library('form_validation');
    }

    public function index()
    {
        $q = urldecode($this->input->get('q', TRUE));
        $start = intval($this->input->get('start'));
        
        if ($q <> '') {
            $config['base_url'] = base_url() . 'results/index.html?q=' . urlencode($q);
            $config['first_url'] = base_url() . 'results/index.html?q=' . urlencode($q);
        } else {
            $config['base_url'] = base_url() . 'results/index.html';
            $config['first_url'] = base_url() . 'results/index.html';
        }

        $config['per_page'] = 10;
        $config['page_query_string'] = TRUE;
        $config['total_rows'] = $this->Results_model->total_rows($q);
        $results = $this->Results_model->get_limit_data($config['per_page'], $start, $q);

        $this->load->library('pagination');
        $this->pagination->initialize($config);

        $data = array(
            'results_data' => $results,
            'q' => $q,
            'pagination' => $this->pagination->create_links(),
            'total_rows' => $config['total_rows'],
            'start' => $start,
        );
        $this->load->view('results/results_list', $data);
    }

    public function read($id) 
    {
        $row = $this->Results_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'status' => $row->status,
		'recommendation' => $row->recommendation,
	    );
            $this->load->view('results/results_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('results'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('results/create_action'),
	    'id' => set_value('id'),
	    'status' => set_value('status'),
	    'recommendation' => set_value('recommendation'),
	);
        $this->load->view('results/results_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'status' => $this->input->post('status',TRUE),
		'recommendation' => $this->input->post('recommendation',TRUE),
	    );

            $this->Results_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('results'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Results_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('results/update_action'),
		'id' => set_value('id', $row->id),
		'status' => set_value('status', $row->status),
		'recommendation' => set_value('recommendation', $row->recommendation),
	    );
            $this->load->view('results/results_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('results'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'status' => $this->input->post('status',TRUE),
		'recommendation' => $this->input->post('recommendation',TRUE),
	    );

            $this->Results_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('results'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Results_model->get_by_id($id);

        if ($row) {
            $this->Results_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('results'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('results'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('status', 'status', 'trim|required');
	$this->form_validation->set_rules('recommendation', 'recommendation', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Results.php */
/* Location: ./application/controllers/Results.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2019-09-12 22:11:55 */
/* http://harviacode.com */